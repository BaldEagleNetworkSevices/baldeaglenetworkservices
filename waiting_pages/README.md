# Waiting Pages

These are inactive pages removed from the active public site.

They are retained for possible future funnel pages, landing pages, service experiments, or service-pivot reuse.

They are not part of the current public website and should not be linked, routed, indexed, or included in the sitemap unless intentionally restored.

Current archived pages:

- `cybersecurity-salt-lake-city`
- `endpoint-management`
- `it-services-salt-lake-city`
- `managed-it-services`
- `managed-it-support-salt-lake-city`
- `microsoft-365-services`
- `microsoft-365-support-salt-lake-city`
- `network-cabling-wifi`
- `one-off-it-projects`
- `voip-business-phone-systems`

Most of these were removed after the business pivot toward backup, restore, verification, and recovery-first service offers.

If a page is restored later:

1. Move its route file back to the repo root if needed.
2. Restore its page definition in `includes/page-definitions.php`.
3. Re-add its page catalog entry in `includes/functions.php` only if it should be publicly routable.
4. Intentionally reconnect navigation, CTA, sitemap, canonical, and redirect behavior.
